export const randomBreathRate = () => {
    return Math.floor(Math.random() * (60 + 1));
}

export const randomOxygenSatLvl = () => {
    return Math.floor(Math.random() * (100 + 1));
}

export const randomheartRate = () => {
    return Math.floor(Math.random() * (200 + 1));
}